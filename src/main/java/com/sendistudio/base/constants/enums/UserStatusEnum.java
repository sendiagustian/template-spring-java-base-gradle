package com.sendistudio.base.constants.enums;

public class UserStatusEnum {
    public static final String NAME = "core.user_status_enum";

    public static enum Enum {
        Active,
        Inactive,
        Suspended
    }
}
