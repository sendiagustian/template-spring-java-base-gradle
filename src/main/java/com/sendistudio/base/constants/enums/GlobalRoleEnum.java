package com.sendistudio.base.constants.enums;

public class GlobalRoleEnum {

    public static final String NAME = "core.global_role_enum";

    public enum Enum {
        Owner,
        Admin,
        Staff
    }
}
