sendistudio:
  jasypt:
    encryptor:
      secret: secret12345

  database:
    local:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:3306}
      user: ${DB_USER:sample_user}
      pass: ${DB_PASS:sample_password}
      name: ${DB_NAME:sample_database}

    dev:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:3306}
      user: ${DB_USER:sample_user}
      pass: ${DB_PASS:sample_password}
      name: ${DB_NAME:sample_database}

    prod:
      host: ${DB_HOST:localhost}
      port: ${DB_PORT:3306}
      user: ${DB_USER:sample_user}
      pass: ${DB_PASS:sample_password}
      name: ${DB_NAME:sample_database}
